import { MovieCard } from "../MovieCard/MovieCard";
import { List } from './MovieList.styled';

export function MovieList() {
    return (
        <List>
            <MovieCard />
        </List>
    )
}